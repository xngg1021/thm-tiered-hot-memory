# THM × Hermes pinned-upstream provider lifecycle E2E

Date: 2026-09-06

This report records an actual GitHub Actions integration run, not a contract double.

- THM source commit exercised: `2d55f944ec066bdb26444db633d2a686fd22ec2d`
- Hermes upstream commit: `77915e344cb0cd8e20661d4a7b393f987a2eef32`
- Workflow run: `34042345020`
- Artifact: `9992058540` (`thm-hermes-e2e`)
- Artifact SHA-256: `8abe37bd87a8b11986cd308cfda418cc0ce966696bbd6f3cdfa0ad44631df154`
- Result: **PASS**

The workflow checked out and installed the pinned Hermes repository, installed THM as a Python package, and used Hermes' real `hermes_agent.memory_providers` entry-point discovery. Hermes loaded `THMProvider` as its actual `MemoryProvider` type and admitted it through the real `MemoryManager` path.

A temporary synthetic recall database contained one fact: the integration database port is `5439`. THM prefetch returned that evidence for the current session; Hermes' real `build_memory_context_block` wrapped it in the memory-context fence; `RecallStatus` and the THM status tool retained the selected source ID. A session switch rejected the old session ID and allowed a fresh recall for the new session.

`on_memory_write` was explicitly checked as a notification rather than a usage hit: it cleared last-recall state but left the derived recall database byte-identical. No native Hermes memory file, real user profile or personal history was used.

## Boundary

This is a **provider discovery and lifecycle E2E**, not a model-answer E2E. The test itself made zero model/API calls. The GitHub workflow necessarily used network access to check out/install the pinned public repositories and dependencies; the `network_calls_by_test: 0` field refers to the Python lifecycle test after installation.

It does not establish real-user answer accuracy, model use of recalled context, user-perceived latency, automatic tier movement, or optimal decay. Those remain separate evidence layers.

Machine-readable record: [2026-09-06-hermes-e2e.json](2026-09-06-hermes-e2e.json).
